print "Enter sentence: ";
String str = System.console().readLine();

for(int a=0 ; a<str.length() ; a++){
        if(str.charAt(a)==' ')
                            println "";
        else
                            print str.charAt(a);
}

println "";
